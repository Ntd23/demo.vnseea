// English description: Defines poke bounded-context types backed by the normalized feed API payload.

export type {
  FeedPokeActionResult as PokeActionResult,
  FeedPokeRecord as PokeRecord,
} from "../../../feed/domain/types/feed.types"
